from django.db import models
from django.contrib.auth.models import AbstractUser # Use Django's built-in User model initially

    # We'll use Django's default User model for now.
    # If you decide to extend it, uncomment and modify this:
    # class User(AbstractUser):
    #     # Add any custom fields here, e.g., phone_number, address, etc.
    #     pass
