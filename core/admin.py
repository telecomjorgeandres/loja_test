from django.contrib import admin
from django.contrib.auth.admin import UserAdmin
#from .models import User # Assuming a custom User model if created later, else remove

# Register your models here.
#admin.site.register(User, UserAdmin) # Uncomment if you define a custom User model
